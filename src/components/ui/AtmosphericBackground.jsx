/**
 * AtmosphericBackground Component - DISABLED FOR PERFORMANCE
 * Re-enable after performance testing
 */

export default function AtmosphericBackground() {
    // TEMPORARILY DISABLED - This component runs 4 large gradient animations
    // on fixed position across the ENTIRE page, causing GPU overload
    return null
}
